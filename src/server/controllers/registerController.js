exports.registerUser = async (req, res) => {
  // Implement registration logic:
  // 1. Validate input
  // 2. Hash password
  // 3. Save user to database
  // 4. Handle any errors
  // 5. Redirect to home page or user's page

  res.redirect("/")
}